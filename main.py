import max_profit as mp
import prime_number as pn

if __name__ == '__main__':
    pn.is_prime_number(1)
    pn.is_prime_number(10)
    pn.is_prime_number(21)
    pn.is_prime_number(29)
    pn.is_prime_number(32)
    pn.is_prime_number(74)
